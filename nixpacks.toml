[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["echo 'VITE_API_URL=https://dinamicas-back-end-production.up.railway.app' > .env", "npm run build"]

[start]
cmd = "npx serve dist -p $PORT"